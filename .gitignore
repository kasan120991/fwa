# OS
.DS_Store

# Editors / IDE tooling
.idea
.fleet
.vscode

# Junie
.junie

# Node dependencies (per-package .gitignore also covers subtrees)
node_modules

# Logs
*.log

# Local env files
.env
.env.*
!.env.example

# Design-system skill — a symlink to the (gitignored, local-only) design system
.claude/skills/fwa-design

# Playwright MCP session artifacts (screenshots, snapshots, console logs)
.playwright-mcp

# Marketing website — its own git repo, tracked separately (see website/CLAUDE.md)
website/
